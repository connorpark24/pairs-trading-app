{
  "name": "pairs-trading-webapp",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {}
}
